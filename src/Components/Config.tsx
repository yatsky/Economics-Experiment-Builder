const drawerWidth = 140;

// VariableType for PageElement
enum VariableType {
    StringVariable,
    IntegerVariable,
    CurrencyVariable,
    BooleanVariable,
    // This is pure text
    PureText,
}

export { drawerWidth, VariableType };